using UnityEngine;

namespace LDtkUnity
{
    /// <summary>
    /// Used strictly for projects that don't use external levels.
    /// //AFTERTHOUGHT may never end up using since the data will always end up into the project json regardless of external levels or not
    /// </summary>
    /*public class LDtkComponentLevel : LDtkJsonComponent<Level, LDtkLevelFile>
    {
    }*/
}
